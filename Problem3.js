//Put the start of your program in a main function. Create an attendance array. Ask the user if a teacher, student, or
// parent is checking in. Use a switch statement to add the teacher/student/parent to the attendance array, then log the
// option they chose. Repeat this five times.
//
// Challenge: Do the above program until the user enter's 'q'. Also, print and add to the array via a function call to
// the appropriate teacher/student/parent function.


main();
    function main()
    {
        attendance = [];
        var attendance = ["teacher", "student", "parent"];





        var attendance = prompt("Who is checking in?");
        switch (attendance)
        {
            case "[attendance]":
                console.log("");
                break;
            case "[attendance]":
                console.log("");
                break;
            case "[attendance]":
                console.log("");
                break;
            case "[attendance]":
                console.log("");
                break;
            case "[attendance]":
                console.log("");
                break;
            default:
                console.log("error");












        }






    }